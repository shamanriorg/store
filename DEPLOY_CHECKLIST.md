# Чеклист для проверки перед деплоем

## ✅ Локальное тестирование статической версии

### 1. Генерация статического сайта
```bash
npm run generate
```

### 2. Локальный просмотр статической версии
```bash
# Вариант 1: Используя встроенный preview (рекомендуется)
npm run preview

# Вариант 2: Используя serve (более реалистично для GitHub Pages)
npm run preview:static
# Откроется на http://localhost:3000 (или другой порт)
```

### 3. Что проверить локально:

#### ✅ Базовые страницы
- [ ] Главная страница (`/`) загружается
- [ ] Каталог (`/catalog`) загружается
- [ ] Блог (`/blog`) загружается
- [ ] О нас (`/about`) загружается
- [ ] О компании (`/company`) загружается

#### ✅ Динамические страницы
- [ ] Страницы товаров (`/product/patterns/{id}`) загружаются
- [ ] Страницы статей (`/blog/{id}`) загружаются
- [ ] SEO теги правильные на каждой странице
- [ ] Изображения загружаются

#### ✅ Навигация
- [ ] Переходы между страницами работают
- [ ] Нет мелькания 404 при навигации
- [ ] Все ссылки работают

#### ✅ Функциональность
- [ ] Каталог фильтруется по категориям
- [ ] Корзина работает (на клиенте)
- [ ] Формы работают

#### ✅ SEO
- [ ] Sitemap доступен (`/sitemap.xml`)
- [ ] Robots.txt доступен (`/robots.txt`)
- [ ] Мета-теги правильные

### 4. Проверка в браузере
1. Откройте DevTools (F12)
2. Проверьте Console на ошибки
3. Проверьте Network - все ресурсы загружаются
4. Проверьте, что нет 404 ошибок

## 🚀 Деплой на GitHub Pages

### Автоматический деплой
1. Убедитесь, что workflow файл `.github/workflows/deploy.yml` настроен
2. Push в ветку `main` автоматически запустит деплой
3. Проверьте Actions в GitHub для статуса деплоя

### Ручная проверка после деплоя
1. Откройте сайт на GitHub Pages
2. Проверьте все страницы
3. Проверьте консоль браузера на ошибки
4. Проверьте, что все изображения загружаются

## ⚠️ Возможные проблемы

### Проблема: Пути к ресурсам
- **Решение**: Все пути должны быть относительными или начинаться с `/`
- **Проверка**: Откройте сгенерированный HTML и проверьте пути к CSS/JS

### Проблема: Изображения не загружаются
- **Решение**: Убедитесь, что изображения в `public/` и пути правильные
- **Проверка**: Проверьте Network tab в DevTools

### Проблема: API запросы не работают
- **Решение**: В статической версии нет сервера, только статические файлы
- **Проверка**: Все данные должны быть загружены через `useAsyncData` или статические файлы

## 📝 Команды для тестирования

```bash
# Полная проверка: генерация + локальный просмотр
npm run test:generate

# Только генерация
npm run generate

# Просмотр сгенерированного сайта
npm run preview
# или
npm run preview:static
```
