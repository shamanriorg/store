# ✅ Проверка перед деплоем

## 🎯 Главное правило

**`npm run dev` ≠ `npm run generate`**

- `npm run dev` - запускает SSR сервер (как на проде НЕ будет)
- `npm run generate` - создает статические HTML файлы (как на проде БУДЕТ)

**Обязательно тестируйте статическую версию перед деплоем!**

## 🧪 Быстрая проверка (5 минут)

### 1. Генерация
```bash
npm run generate
```
✅ Должно быть: `Prerendered 95 routes` (без ERROR)

### 2. Локальный просмотр
```bash
npm run preview
```
Откроется на `http://localhost:4173`

### 3. Проверка в браузере
- [ ] Откройте несколько страниц (главная, каталог, товар, статья)
- [ ] Откройте DevTools (F12) → Console - нет ошибок
- [ ] Network tab - все ресурсы загружаются (200 OK)
- [ ] Проверьте SEO теги: View Source → проверьте `<title>` на разных страницах

### 4. Проверка файлов
```bash
# Проверьте, что sitemap создан
Test-Path .output\public\sitemap.xml

# Проверьте, что robots.txt создан
Test-Path .output\public\robots.txt
```

## ✅ Готово к деплою, если:

- ✅ `npm run generate` завершается без ERROR
- ✅ `npm run preview` показывает рабочий сайт
- ✅ Нет ошибок в консоли браузера
- ✅ Все страницы загружаются
- ✅ SEO теги правильные на каждой странице
- ✅ Sitemap и robots.txt созданы

## 🚀 Деплой

1. Commit и push в `main`
2. GitHub Actions автоматически задеплоит
3. Проверьте сайт на проде после деплоя

## ⚠️ Если что-то не работает на проде

1. Проверьте логи GitHub Actions
2. Запустите `npm run generate` локально и проверьте ошибки
3. Сравните локальную статическую версию (`npm run preview`) с продом
