# 🔧 Решение ошибки EBUSY при генерации

## Проблема
```
ERROR  EBUSY: resource busy or locked, rmdir 'D:\Users\lucky\Documents\git\shamanri\.output'
```

## Причина
Директория `.output` заблокирована каким-то процессом (dev сервер, preview сервер, VS Code/Cursor, антивирус).

## Решения (по порядку)

### 1. Закрыть все процессы Node.js
```powershell
# Остановить все процессы node
Get-Process node | Stop-Process -Force

# Подождать 2 секунды
Start-Sleep -Seconds 2

# Запустить генерацию
npm run generate
```

### 2. Закрыть VS Code/Cursor и перезапустить
1. Закройте VS Code/Cursor полностью
2. Откройте терминал (PowerShell/CMD) вне редактора
3. Перейдите в директорию проекта
4. Запустите `npm run generate`

### 3. Перезагрузить компьютер
Если ничего не помогает, перезагрузите компьютер и попробуйте снова.

### 4. Игнорировать ошибку (если генерация все равно работает)
Если после ошибки EBUSY генерация все равно завершается успешно (`Prerendered X routes`), можно игнорировать эту ошибку - она не критична.

### 5. Использовать другой терминал
Попробуйте запустить `npm run generate` в другом терминале (не в Cursor/VS Code).

## Проверка после исправления
```bash
# Проверьте, что генерация прошла успешно
npm run generate

# Должно быть: "Prerendered 95 routes" (без ERROR)
```

## Альтернатива: Использовать WSL (если установлен)
Если у вас установлен WSL, можно запустить генерацию там:
```bash
wsl
cd /mnt/d/Users/lucky/Documents/git/shamanri
npm run generate
```
