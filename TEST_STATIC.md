# 🧪 Тестирование статической версии перед деплоем

## ⚠️ Важно!

При разработке через `npm run dev` используется SSR сервер, а на проде будет статический HTML. 
**Обязательно тестируйте статическую версию локально перед деплоем!**

## 📋 Пошаговая инструкция

### 1. Генерация статического сайта
```bash
npm run generate
```

**Проверьте вывод:**
- ✅ Должно быть: `Prerendered X routes` (без ERROR)
- ❌ Если есть ERROR - исправьте перед деплоем

### 2. Локальный просмотр статической версии

#### Вариант A: Используя встроенный preview (рекомендуется)
```bash
npm run preview
```
Откроется на `http://localhost:4173` (или другой порт)

#### Вариант B: Используя serve (более реалистично)
```bash
# Установите serve, если еще не установлен
npm install -g serve

# Запустите статический сервер
npm run preview:static
# или
serve .output/public
```

### 3. Что проверить

#### ✅ Базовые страницы
- [ ] Главная (`/`) - загружается, нет ошибок в консоли
- [ ] Каталог (`/catalog`) - товары отображаются
- [ ] Блог (`/blog`) - статьи отображаются
- [ ] О нас (`/about`) - загружается
- [ ] О компании (`/company`) - загружается

#### ✅ Динамические страницы
- [ ] Откройте несколько товаров (`/product/patterns/{id}`)
- [ ] Откройте несколько статей (`/blog/{id}`)
- [ ] Проверьте SEO теги в `<head>` (View Source)
- [ ] Изображения загружаются

#### ✅ Навигация
- [ ] Переходы между страницами работают
- [ ] Нет мелькания 404 при навигации
- [ ] Все ссылки кликабельны

#### ✅ Функциональность
- [ ] Каталог фильтруется по категориям
- [ ] Корзина работает (добавление товаров)
- [ ] Формы работают (если есть)

#### ✅ SEO файлы
- [ ] Sitemap доступен: `http://localhost:4173/sitemap.xml`
- [ ] Robots.txt доступен: `http://localhost:4173/robots.txt`

#### ✅ Консоль браузера
1. Откройте DevTools (F12)
2. Вкладка Console - не должно быть ошибок
3. Вкладка Network - все ресурсы загружаются (статус 200)
4. Проверьте, что нет 404 ошибок

### 4. Проверка в разных браузерах
- [ ] Chrome/Edge
- [ ] Firefox
- [ ] Safari (если есть Mac)

### 5. Проверка на мобильных устройствах
- [ ] Откройте DevTools → Device Toolbar
- [ ] Проверьте на разных размерах экрана
- [ ] Проверьте мобильное меню

## 🔍 Типичные проблемы и решения

### Проблема: Пути к ресурсам не работают
**Симптом:** CSS/JS/изображения не загружаются (404)
**Решение:** Все пути должны быть относительными или начинаться с `/`
**Проверка:** Откройте Network tab, проверьте пути к ресурсам

### Проблема: Страницы товаров/статей не загружаются
**Симптом:** Показывается 404 или пустая страница
**Решение:** Проверьте, что файлы `product.json` и `article.json` существуют
**Проверка:** `npm run generate` должен успешно завершиться

### Проблема: SEO теги общие, а не уникальные
**Симптом:** Все страницы имеют одинаковый title
**Решение:** Убедитесь, что `useAsyncData` загружает данные на сервере
**Проверка:** View Source страницы, проверьте `<title>` и `<meta>` теги

### Проблема: Изображения не загружаются
**Симптом:** Изображения показывают placeholder или 404
**Решение:** Проверьте пути к изображениям в `product.json` и `article.json`
**Проверка:** Network tab → Images → проверьте статус загрузки

## 🚀 После успешного тестирования

1. ✅ Убедитесь, что все проверки пройдены
2. ✅ Commit и push в `main`
3. ✅ Проверьте GitHub Actions - деплой должен пройти успешно
4. ✅ После деплоя проверьте сайт на проде

## 📝 Быстрая команда для полной проверки

```bash
# Генерация + автоматический просмотр
npm run test:generate
```

Это сгенерирует сайт и запустит локальный сервер для проверки.
